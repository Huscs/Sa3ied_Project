<h5 align="center">Search:Volunteers</h5>
<div align="center">
  <%= form_for(:search, :html => {class: "form-horizontal", role: "form"}, url: search_vol_path) do |f| %>

  <% @users.each do |user| %>
    
    <ul class="listing">
      <div class="row">
        <div class="well col-md-4 col-md-offset-4">
            <li><%= link_to gravatar_for(user), user_path(user) %></li>
                <li class="user-title">
                <h2><%=user.username%></h2>
            </li>
            <br></br>

            <li>
              <h1>Rating: <%= user.rating %></h1>
              
            </li>
              
        </div>
              
      </div>
    </ul>
  <% end %>
  <% end %>
</div>